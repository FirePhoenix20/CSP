<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        /* BEGIN: CSS for dark theme */
        body {
            font-family: 'Roboto', sans-serif; /* Changed font to Roboto */
            background-color: #121212; /* Dark background */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            color: #ffffff; /* Light text color */
        }
        form {
            background: #292929; /* Darker form background */
            padding: 20px;
            border-radius: 22px; /* More rounded corners */
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
        }
        label {
            display: block;
            margin-bottom: 10px;
            color: #e0e0e0; /* Light color for labels */
        }
        input[type="text"],
        input[type="password"] {
            width: 93%;
            transition: all 0.5s ease;
            padding: 10px;
            margin-bottom: 15px;
            margin-right: 150px;
            border: 1px solid #444; /* Dark border */
            border-radius: 14px;
            background-color: #2a2a2a; /* Dark input background */
            color: #ffffff; /* Light text color for inputs */
        }
        button {
            width: 100%;
            padding: 10px;
            border: 2px solid #44444467;
            border-radius: 4px;
            background-color: #06ebb9; /* Primary button color */
            color: rgb(0, 0, 0);
            font-size: 16px;
            border-radius: 20px;
            cursor: pointer;
            margin-bottom: 10px;
            transition: all 0.5s ease;
        }
        button[type="button"] {
            background-color: #c0c0c0; /* Secondary button color */
        }
        button:hover {
            opacity: 0.5;
            border: 2px solid #ffffff;
        }
        /* END: CSS for dark theme */
    </style>
</head>
<body>
  
    <form action="/login" method="post"> <!-- BEGIN: Updated action URL -->
        <label for="username">Username:</label>
        <input type="text" id="username" name="username" required><br>
        
        <label for="password">Password:</label>
        <input type="password" id="password" name="password" required><br>
        
        <button type="submit">Sign In</button>
        <button type="button" onclick="window.location.href='create_account.html'">Create New Account</button>
    </form>
</body>
</html>
